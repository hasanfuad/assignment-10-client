import React from 'react';
import Button from "react-bootstrap/Button";

const Login = () => {
    return (
        <div>
            <h1>This is Login</h1>
            <Button className="btn btn-bg-primary mt-2 p-2">Click to the link</Button>
        </div>
    );
};

export default Login;